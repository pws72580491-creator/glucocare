# CHANGELOG

## v1.5.0 — 2026-09-13

꼼꼼한 검수·리뷰에서 나온 개선 항목과, 운영(관리) 측면 추천을 모두 반영했습니다.

### 추가
- **전체 데이터 백업/복원(JSON)**: CSV 내보내기는 최근 30일·인쇄용이라 백업으로
  쓰기엔 부족했고, 가져오기(import) 기능도 없어서 기기를 바꾸거나 브라우저
  데이터가 지워지면 기록을 복구할 방법이 없었습니다. "공유" 탭에 "전체 데이터
  백업(JSON)" / "백업 파일에서 복원" 버튼을 추가했습니다. 백업은 기간 제한 없이
  전 저장소를 담고, 복원은 기존 id를 버리고 새 기록으로 추가하는 방식이라
  지금 기기의 데이터와 충돌하지 않습니다(단, 같은 백업을 두 번 복원하면
  중복이 생기므로 복원 전 확인창을 띄웁니다).
- **`verify.js`**: 그동안 체인지로그의 "N개 시나리오로 확인"을 다시 실행해볼 방법이
  없었습니다. 의존성 없이 `node verify.js`로 바로 도는 회귀 검증 스크립트를
  추가했습니다 — FoodDB(음식 매칭·식사 점수)와 Insights(eA1C·통계·목표범위)의
  순수 로직 19개 시나리오를 검증합니다. DOM/IndexedDB에 의존하는 app.js·db.js는
  포함하지 않았습니다(빌드 과정 없는 구조를 유지하기 위해 jsdom 등을 새로
  들이지 않음).

### 수정
- **목표범위(70~140) 하드코딩 3중화 해소**: `insights.js`의 `GLUCOSE_TARGET`을
  그래프 탭 차트는 참조했지만, 리포트 탭 분포차트는 70/140/180을 따로
  하드코딩했고 HTML의 "목표 70–140" 라벨도 별도 텍스트였습니다. `insights.js`에
  `GLUCOSE_ZONES`(구간 정의)를 추가해 리포트 차트가 이를 참조하도록 바꾸고,
  HTML 라벨도 `renderCharts()`에서 `GLUCOSE_TARGET` 값으로 채우도록
  고쳤습니다. 목표범위를 나중에 측정 시점별로 나누더라도(README에 이미 적힌
  개선 방향) 한 곳만 고치면 됩니다.
- **AI 사진분석 note 연결**: `api/analyze-meal.js`가 만들어 응답에 포함시키던
  한 줄 설명(note)을 `app.js`가 받아놓고 화면에 표시하지 않았습니다. 인식된
  음식 목록 아래에 표시하도록 연결하고, 그동안 항상 빈 문자열로만 저장되던
  `meal.photoNote` 필드에 실제로 담기도록 고쳤습니다(수정 화면에서도 복원됨).
  CSV 내보내기에도 "사진분석메모" 칼럼을 추가해 누락되지 않게 했습니다.

### 개선
- **차트 탭 resize에 디바운스(150ms) 추가**: 혈압·혈당 트래커 Pro에는 이미 있던
  패턴인데 이 앱엔 없었습니다. 창을 드래그로 리사이즈할 때마다 IndexedDB
  재조회 + 캔버스 3개 재렌더링이 그대로 반복되던 걸 막았습니다.
- **`analyze-meal` 엔드포인트 최소 보호**: 인증이 없는 엔드포인트라 호출마다
  Gemini 과금이 발생하는데, URL만 알면 누구든 호출할 수 있었습니다. 같은
  오리진 확인(다른 사이트에서의 직접 호출 차단)과 IP당 5분/8회 인메모리 요청
  제한을 추가했습니다. 서버리스 특성상 완벽한 방어는 아니라서(콜드스타트마다
  리셋) Vercel 함수 호출 수·Gemini 사용량 대시보드를 함께 확인하는 걸
  권장합니다.

### 정리
- 미사용 CSS 제거: `.card-grid`/`.stat-card`(예전 대시보드 레이아웃, 지금은
  `.mini-stats`/`.item` 사용), `.btn.danger-text`, `.field .unit-suffix` — 총
  네 종류, 어디서도 참조되지 않던 규칙들입니다. (`.partner-row` 등 "연결된
  보호자" 관련 클래스는 다음 기능을 위해 미리 마련해 둔 것으로 보여 남겨뒀습니다.)
- `foodDb.js` 상단 주석이 v1.1.0 이전 상태를 설명하고 있었습니다("실제 사진
  인식은 API 없인 불가능", 존재하지 않는 `js/aiVision.example.js` 참고 안내).
  실제로는 `api/analyze-meal.js`가 이미 그 역할을 하고 있어서, 지금 구조를
  설명하도록 다시 썼습니다.
- 테스트: `verify.js` 19개 시나리오 전체 통과. 그 외 Node 문법 체크, 중복 함수
  검사, HTML 태그 균형, DOM id 참조 정합성, 버전 4곳(app.js/sw.js/README/
  CHANGELOG) 일치, inline onclick 부재를 모두 재확인.



전체 코드를 처음부터 다시 훑어본 리뷰에서 나온 항목들을 반영했습니다.

### 수정
- **식단 값 검증 추가**: 혈당·혈압·체중·운동에는 있던 저장 시 범위 검증이 식단에는
  없었습니다. 탄수화물(0~300g)·단백질(0~200g)·지방(0~200g)·나트륨(0~6000mg)·
  GI(0~100)에 검증을 추가하고, 입력창에도 다른 필드처럼 `min`/`max` 힌트를
  달았습니다. 수동으로 값을 고치다 실수로 음수나 비정상적으로 큰 값을 넣어도
  이제 저장 전에 막힙니다.
- **CSV 내보내기 재설계**: 기존에는 `항목1/항목2/항목3` 같은 공용 칸에 욱여넣다
  보니 식단의 단백질·지방·GI가 아예 빠지고, 식사구분(mealType)이 "메모" 칸에
  잘못 들어가 있었습니다. 이제 타입마다 의미 있는 칼럼(혈당, 측정시점, 수축기,
  이완기, 맥박, 체중, 식사구분, 음식이름, 탄수화물, 단백질, 지방, 나트륨, GI,
  운동종류, 운동시간, 강도, 약품명, 용량, 메모)을 두고 해당 없는 칸은 비워두는
  방식으로 바꿔서, 어떤 기록도 값이 누락되지 않습니다.

### 개선
- 식단 사진은 한 번에 최대 5장까지만 추가할 수 있도록 제한했습니다(사진 1장당
  최대 6개 항목 인식이므로 최악의 경우 30개까지 쌓일 수 있어서). 한도에 도달하면
  촬영/갤러리 버튼이 비활성화되고, 넘겨서 시도하면 안내 메시지가 뜹니다 —
  불필요한 Gemini 호출(비용)을 막기 위함입니다.
- 식사 점수가 이제 지방도 반영합니다(35g 이상이면 감점). 이전에는 지방이 아무리
  많아도 점수에 영향이 없었습니다. 점수 코멘트도 실제로 어떤 요인(GI/나트륨/
  탄수화물/지방) 때문에 감점됐는지 반영하도록 고쳤습니다 — 이전에는 원인과
  무관하게 항상 "혈당지수와 나트륨이 높다"는 문구만 나왔습니다.

### 정리
- 예전 버전의 흔적으로 남아있던 미사용 코드 제거: `state.pendingMealAnalysis`
  (선언과 초기화만 있고 실제로 쓰이는 곳이 없었음), 사용되지 않는 CSS
  `.ai-analysis .nutrient-grid` 관련 규칙 4개(v1.3.0에서 항목별 리스트 방식으로
  바뀌면서 더 이상 해당 마크업이 생성되지 않음).
- 테스트: 식단 검증 경계값 9개, CSV 컬럼 정렬·데이터 배치 22개, 식사 점수의
  지방 반영·동적 코멘트·플로어 값 13개 — 총 44개 시나리오로 확인.

## v1.3.1 — 2026-09-12

식단 이름을 입력하는 도중 엉뚱한 음식이 잘못 매칭되고, 그 값을 지워도 초기화되지
않던 버그를 고쳤습니다. (예: "구운계란"을 입력하는 과정에서 "고구마"가 매칭되어
버림 — "고구마".includes("구")가 참이라서 발생)

### 수정
- `foodDb.js`의 `matchByName()`이 이제 부분일치를 최소 2글자부터만 시도합니다.
  한 글자짜리 검색어(입력 도중의 중간 상태 포함)는 다른 음식 이름 안에 우연히
  포함되는 경우가 많아 엉뚱하게 매칭되기 쉬웠습니다("구" → "고구마" 등). 정확히
  일치하는 경우는 글자 수와 상관없이 그대로 매칭됩니다.
- 인식된 음식 항목을 전부 지우면(✕ 버튼으로 마지막 항목까지 제거) 탄수화물·
  단백질·지방·나트륨·GI 필드가 이제 0으로 초기화됩니다. 이전에는 항목이
  없어져도 직전에 잘못 채워졌던 숫자가 그대로 남아있었습니다. 음식 이름
  필드는 사용자가 입력한 텍스트를 그대로 유지합니다(비우지 않음).
- 식단 이름을 계속 입력하다가(예: 중간에 우연히 매칭됐던 상태에서 계속 타이핑해
  결국 아무것도 안 맞게 된 경우) 더 이상 매칭되는 음식이 없어지면, 직전에
  자동으로 채워졌던 잘못된 값을 자동으로 지웁니다. 단, 사진 분석이나 기존
  기록 수정에서 들어온 항목은 이름 입력과 무관하게 그대로 보존됩니다.
- 테스트: `matchByName`의 1글자 오매칭 차단, 2글자 정상 매칭, 정확 일치,
  빈 문자열 등 9개 시나리오로 재현·검증했습니다.

## v1.3.0 — 2026-09-11

식단 사진 분석 방식을 다시 짰습니다. 이전에는 사진 한 장에 음식이 여러 개
있어도(밥+국+반찬처럼) 하나만 인식했는데, 이제 그릇 단위로 각각 인식해서 합산합니다.

### 추가
- **여러 음식 동시 인식**: Gemini에 "보이는 음식을 모두 항목별로 나눠서 답하라"고
  요청하도록 프롬프트/응답 스키마를 바꿨습니다(`{ name, ... }` 단일 객체 →
  `{ items: [...] }` 배열, 최대 6개). 밥+찌개+반찬이 있으면 각각 인식돼서
  탄수화물·단백질·지방·나트륨이 모두 합산됩니다. GI는 단순 합산이 의미 없는
  값이라, 항목별 탄수화물 비중으로 가중평균해서 식사 전체의 대략적인 값을 냅니다.
- **사진 여러 장 추가 가능**: 한 장 분석 후에도 계속 사진을 추가할 수 있습니다.
  새 사진에서 인식된 항목은 기존 목록에 "추가"됩니다(교체 아님) — 그릇을 따로
  찍어도 다 합산됩니다.
- **인식된 항목 목록 + 항목별 제거**: 분석 카드에 인식된 음식이 목록으로 나오고
  (로컬 테이블 매칭인지 AI 추정인지 배지로 구분), 항목별로 ✕를 눌러 잘못
  인식된 것만 제거할 수 있습니다. 제거하면 나머지 항목으로 합계가 다시 계산됩니다.
- **직접 촬영 지원**: "📷 사진 촬영"(카메라 직접 실행) / "🖼 갤러리에서 선택"
  두 버튼으로 분리했습니다. 이전에는 파일 선택창에서 카메라/갤러리를 매번 골라야
  했는데, 촬영 버튼은 바로 카메라 앱을 엽니다(`capture="environment"`).
- 기존 기록을 수정할 때 사진을 추가하면, 저장된 값이 "기존" 항목으로 먼저
  들어가 있고 새로 인식된 항목이 거기에 더해집니다. 기존 값을 빼고 싶으면
  "기존" 항목을 ✕로 지우면 됩니다.
- 식사 점수는 항상 "지금 입력 필드에 있는 값"(저장될 값) 기준으로 계산됩니다 —
  인식된 항목 목록은 참고용이고, 사용자가 숫자를 직접 고치면 점수도 즉시
  그 값을 반영합니다.
- 인식된 음식 이름(서버·AI가 준 자유 텍스트)도 다른 곳과 동일하게 이스케이프
  후 표시합니다.
- 테스트: 서버 쪽 다중 항목 파싱·클램핑·빈 배열·6개 초과 잘라내기 등 27개,
  `computeMealTotals`의 합산·가중평균 GI·0-탄수화물 예외처리 14개, 새 사진
  UI 마크업 검증 8개 — 기존 테스트에 추가로 검증.

### 알아두면 좋은 점
- 데이터 모델 자체(하나의 식단 기록 = 이름 문자열 하나 + 합산된 영양 수치)는
  바꾸지 않았습니다. 항목별로 따로 저장하려면(예: "밥"과 "찌개"를 별개 기록으로)
  DB 스키마 변경과 리포트·CSV 내보내기·인지 매칭 로직 전체 수정이 필요해서
  이번 범위에는 포함하지 않았습니다. 지금은 "여러 음식을 인식해서 합친 총량"을
  하나의 식단 기록으로 저장하는 방식입니다.

## v1.2.0 — 2026-09-11

기록을 수정할 수 있게 됐습니다. 이전에는 삭제(✕) 후 다시 입력하는 방법뿐이었습니다.

### 추가
- 대시보드/전체 기록 목록에서 기록 한 줄을 탭하면 그 기록의 값이 미리 채워진 채로
  수정 폼이 열립니다 (삭제 버튼 ✕는 그대로 유지 — 목록 오른쪽 끝에서 바로 삭제 가능).
- `js/db.js`에 `get(store, id)` / `update(store, record)` 추가 — `update()`는
  IndexedDB `put()`으로 기존 id 자리에 덮어써서, 새 기록이 중복 생성되지 않습니다.
- 모든 기록 유형(혈당/혈압/체중/식단/운동/약물)에서 수정 지원. 폼 제목에 "수정"이
  붙고 버튼도 "수정 완료"로 바뀌어 새로 추가하는 것과 헷갈리지 않게 했습니다.
- 수정 폼에 기존 메모·이름·용량 등 자유 입력 텍스트를 채워 넣을 때도 다른 곳과
  동일하게 이스케이프합니다(칩 선택 필드의 숨은 입력값 포함) — 목록에 표시할 때만
  이스케이프하고 수정 폼에는 그대로 꽂아 넣으면 같은 종류의 취약점이 새로 생기므로,
  같은 원칙을 폼 미리채움에도 동일하게 적용했습니다.
- 테스트: `db.js`의 `get`/`update` 9개, `formHtml`/`chipGroup`의 신규 작성·수정
  분기·값 미리채움·XSS 이스케이프 21개 — 총 30개 시나리오 검증.

## v1.1.3 — 2026-09-10

Gemini API가 503(일시적 과부하)을 반환할 때 바로 포기하던 것을, 짧게 재시도하도록
고쳤습니다.

### 수정
- 429/500/502/503/504나 타임아웃, 네트워크 예외는 같은 모델로 0.7초 뒤 한 번 더
  시도합니다(모델당 최대 2번 시도). 그래도 실패하면 다음 모델(v1.1.2에서 추가한
  폴백)로 넘어갑니다 — 최악의 경우 모델 2개 × 시도 2번 = 최대 4번 시도.
- 404(모델 자체가 없음)는 여전히 재시도하지 않고 바로 다음 모델로 넘어갑니다 —
  없는 모델을 다시 물어봐도 의미가 없으니까요.
- 400/403처럼 재시도해도 소용없는 오류는 이전처럼 즉시 실패 처리합니다.
- `vercel.json`의 `maxDuration`을 30초 → 45초로 늘렸습니다 (최대 4번 시도 × 8초 +
  재시도 대기시간을 감안한 여유).
- 테스트: 503 재시도 후 성공, 모델A 재시도 소진 후 모델B로 폴백, 전부 실패 시
  502, 404는 재시도 없이 즉시 폴백, 재시도 불가능한 오류(400)는 즉시 실패, 특정
  모델 강제 지정 시 그 모델 안에서만 재시도, 타임아웃도 재시도 대상 — 8개 시나리오
  추가 검증.

## v1.1.2 — 2026-09-09

`gemini-2.5-flash`가 이미 서비스 종료돼서 사진 분석이 전부 404로 실패하고 있었습니다
(구글이 2026년 안에 모델 라인업을 2.x → 3.x대로 넘김). 모델명을 최신으로 바꾸고,
앞으로 또 이런 일이 생겨도 코드를 다시 배포하지 않고 넘어갈 수 있도록 자동 폴백을
추가했습니다.

### 수정
- 기본 모델을 `gemini-2.5-flash` → `gemini-3.8-flash`로 변경 (2026-09 기준 최신,
  공식 문서에 이미지 분석 예제로 명시된 모델)
- 기본 모델이 404(모델 자체가 없어짐)를 반환하면 자동으로 `gemini-3.6-flash`로 한 번
  더 시도 — 이후 구글이 또 모델을 은퇴시켜도 둘 다 죽기 전까지는 코드 수정 없이 버팁니다
- `GEMINI_MODEL` 환경변수를 명시적으로 설정하면 그 모델 하나만 쓰고 자동 폴백은 하지
  않습니다 (특정 모델을 강제로 고정하고 싶을 때)
- 두 모델 모두 404면, 에러 메시지에 "GEMINI_MODEL 환경변수나 코드의 DEFAULT_MODELS를
  최신 모델명으로 바꿔주세요"라는 안내와 모델 목록 문서 링크를 포함
- 요청당 타임아웃을 25초 → 12초로 줄임 (최대 2번 시도하므로 합산 최대 24초, Vercel
  함수 제한 30초 안에 여유 있게 들어오도록)
- 테스트: 첫 모델 404→둘째 모델 성공, 둘 다 404, `GEMINI_MODEL` 강제 지정 시 폴백
  안 함, 404가 아닌 다른 오류(429 등)는 폴백하지 않고 즉시 실패 — 4개 시나리오 추가
  검증 (기존 12개 + 새 12개 어서션 모두 통과)

## v1.1.1 — 2026-09-08

`api/analyze-meal.js`에 로그를 추가했습니다. 실패 시 사용자에게는 여전히 짧은 안내
토스트만 보이지만, Vercel 대시보드 → Deployments → 해당 배포 → Functions(로그)에서
어떤 단계(키 미설정/Gemini 오류/응답 파싱 실패/타임아웃/예외)에서 왜 실패했는지
자세히 확인할 수 있습니다. 성공 시에도 인식된 음식 이름과 신뢰도가 로그에 남습니다.
동작 자체(요청/응답 형식, 검증 로직)는 v1.1.0과 동일합니다.

## v1.1.0 — 2026-09-08

식사 사진을 실제로 분석하는 기능을 추가했습니다 (기존에는 미리보기만 되고
"Vision API 연동 후 지원" 안내만 떠 있었습니다).

### 추가
- `api/analyze-meal.js` — Vercel 서버리스 함수. 식사 사진을 받아 Gemini Vision으로
  음식 이름과 탄수화물/단백질/지방/나트륨/GI를 추정해서 반환합니다. `GEMINI_API_KEY`는
  이 함수(서버) 안에만 존재하고 브라우저로는 절대 내려가지 않습니다.
- `js/app.js`: 사진 선택 시 자동으로 리사이즈(긴 변 1024px)·JPEG 재압축 후 위 함수를
  호출하고, 반환된 음식 이름을 먼저 로컬 `FoodDB` 테이블과 대조 → 일치하면 검수된
  로컬 값을, 없으면 AI 추정치를 채워 넣습니다("🤖 AI 추정"으로 구분 표시).
- 실패 처리: API 키 미설정, 네트워크 오류, Vercel이 아닌 정적 호스팅(서버리스 함수 없음),
  Gemini 응답 파싱 실패 등 모든 경우에 안내 토스트를 띄우고 기존의 수동 이름 입력 방식으로
  자동 전환됩니다 — 실패해도 앱이 멈추지 않습니다.
- AI가 반환한 설명 텍스트(`note`)도 다른 사용자 입력과 동일하게 이스케이프 후 표시합니다.
- `vercel.json`에 `api/analyze-meal.js`용 `maxDuration: 30` 설정 추가 (Vercel 함수
  기본 제한 10초보다 여유를 둠).

### 참고
- 사진은 분석을 위해 Google Gemini API로 전송됩니다(기록에는 이름·영양 수치만 저장,
  사진 자체는 저장 안 함). 자세한 설정 방법과 주의사항은 README의
  "사진으로 AI 식단 분석" 섹션 참고.
- 테스트 방식: 실제 Gemini API 키 없이도 검증 가능하도록, `fetch`를 모의(mock) 처리해
  성공/실패/타임아웃/잘못된 응답 등 12개 시나리오에 대해 서버리스 함수의 상태 코드와
  응답 값을 확인했습니다. 실제 Gemini 응답 품질(음식 인식 정확도)은 실제 키로 배포한
  뒤 확인이 필요합니다.

## v1.0.2 — 2026-09-07

혈압·혈당 트래커 Pro에서 먼저 검증된 보안·검증 패턴을 이식한 하드닝 릴리스입니다.

### 수정
- 식사 이름·운동 종류·약품명·약물 메모 등 사용자 입력 텍스트를 화면에 표시하기 전에
  HTML 이스케이프하도록 수정 (`js/app.js`의 `describeRecord()` — XSS 방지)
- 혈당·혈압·체중·운동 시간 저장 시 HTML `min`/`max`만으로는 막지 못하던 값(우회 제출,
  일부 모바일 브라우저의 느슨한 검증)을 JS 레벨에서 한 번 더 검증하도록 `validateRecord()` 추가
- 최초 실행 시 자동으로 채워지던 체험용 샘플 데이터를 "체험 데이터로 둘러보기" 버튼을
  눌러야 채워지는 opt-in 방식으로 변경 (`js/db.js`의 `seedIfEmpty()`는 이제 자동 호출되지 않음)
- 체험용 샘플 데이터에 `demo: true` 태그를 붙이고, "공유" 탭에서 "체험 데이터만 삭제"로
  실제 기록은 보존한 채 골라서 지울 수 있는 `DB.clearDemoData()` / `DB.hasDemoData()` 추가
- 마스커블 아이콘을 안전영역 여백을 둔 별도 파일(`icon-192-maskable.png`,
  `icon-512-maskable.png`)로 새로 만들어 매니페스트의 `any`/`maskable` purpose를 분리
  (기존에는 가장자리까지 꽉 찬 아이콘 하나를 두 용도로 겸용해 안드로이드에서 잘릴 수 있었음)
- 서비스워커 캐시 이름을 버전에 맞춰 올림 (`glucocare-pro-v1` → `glucocare-pro-v1_0_2`) —
  다음 배포부터는 이 값을 버전마다 올려주세요

## v1.0.3 — 2026-09-07

### 추가
- "공유" 탭 하단에 버전 표시 추가 (`글루코케어 Pro · v1.0.3`) — 혈압·혈당 트래커 Pro와 동일한
  방식. `js/app.js` 상단의 `APP_VERSION` 하나만 바꾸면 화면 표시도 같이 바뀝니다.

## v1.0.1 — 2026-09-06

### 추가
- `vercel.json` 추가 (서비스워커 캐시 무효화 헤더, manifest 콘텐츠 타입)
- README에 Vercel 배포 3가지 방법 문서화: Drop(드래그앤드롭), CLI(버전 이어서 배포), Git 연동(자동 배포)
- zip 구조를 최상위에 `index.html`이 오도록 변경 (Vercel Drop 등 정적 호스팅에 바로 대응하기 위함 — 이전 버전은 `glucocare/` 폴더로 한 번 감싸져 있었음)

## v1.0.0 — 2026-09-06

최초 릴리스.

### 추가
- 혈당·혈압·체중·식단·운동·약물 통합 기록 (IndexedDB 오프라인 저장)
- 대시보드: 오늘 평균 혈당, 목표범위 상태, 하루 타임라인(식사·혈당 동시 표시)
- 식단 기록: 사진 미리보기 + 음식 이름 기반 영양 자동 매칭(로컬 테이블) + 식사 점수
- 식사 기록에 식후 혈당 변화(Δ) 자동 표시
- 그래프: 7/30/90일 혈당·혈압·체중 추이 (캔버스 렌더링, 외부 라이브러리 없음)
- 예상 당화혈색소(eA1C) 카드
- 리포트: 기간 통계 요약, 혈당 분포 차트, 인쇄/PDF 저장, CSV 내보내기
- 공유 코드 생성 UI (로컬 저장, 실시간 동기화는 다음 버전 예정)
- PWA 매니페스트 + 서비스워커(오프라인 앱 셸)
- 최초 실행 시 체험용 샘플 데이터 자동 시딩 (기존 기록이 없을 때만)

### 알려진 제한사항
- 사진의 실제 AI 자동 인식(비전 모델), CGM 실기기 연동, 애플 건강/헬스 커넥트/핏빗 연동,
  가족·의료진 실시간 공유는 UI/데이터 구조만 준비되어 있고 외부 API·네이티브 연동이 필요합니다.
  자세한 내용은 README.md의 "자리만 마련되어 있고, 연동이 필요한 기능" 참고.
